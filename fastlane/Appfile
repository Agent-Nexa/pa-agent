# Customise this file, documentation can be found here:
# https://docs.fastlane.tools/advanced/#appfile

app_identifier("z.Nexa") # Change if this isn't the bundle identifier

# You can uncomment the lines below and add your Apple ID if you prefer 
# not to rely entirely on the API Key setup, but it's strongly recommended 
# for CI/CD like GitHub Actions to use API keys (like the Fastfile approach).
#
# apple_id("yourappleid@example.com") 
# itc_team_id("YOUR_ITC_TEAM_ID") 
# team_id("YOUR_DEV_TEAM_ID") 
